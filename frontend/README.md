# AI Document Q&A Frontend

## Setup

1. Run `npm install` to install dependencies.
2. Start the app with `npm run dev`.

## Features
- Upload PDF (left panel)
- Chat with document (right panel)
- Responsive, minimal UI
